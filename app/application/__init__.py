"""Application layer.

Use-cases orchestration lives here (coordination of Domain + Infrastructure).

Note: `app/services` is kept temporarily as a compatibility shim, but routers should
import from `app.application` going forward.
"""
